# Transcription Compatibility Contract

Metadata:
- Purpose: Point old transcription contract path to the active shared module.
- Phase: Architecture migration.
- Last updated: 2026-06-01.
- Notes: Runtime implementation lives in `shared/speech_to_text/`.

The active contract is:

```text
shared/speech_to_text/CONTRACT.md
```

The `modules.transcription` path is retained only as a compatibility wrapper
during migration.
