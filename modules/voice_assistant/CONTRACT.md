# Voice Assistant Compatibility Contract

Metadata:
- Purpose: Point old voice assistant contract path to the active interface module.
- Phase: Architecture migration.
- Last updated: 2026-06-01.
- Notes: Runtime implementation lives in `interfaces/voice/assistant/`.

The active contract is:

```text
interfaces/voice/assistant/CONTRACT.md
```

The `modules.voice_assistant` path is retained only as a compatibility wrapper
during migration.
